# Required: Vercel personal or team access token with deployment check scope.
# Create at https://vercel.com/account/tokens
VERCEL_ACCESS_TOKEN=

# Optional: port the local handler listens on. Defaults to 3000.
PORT=3000
